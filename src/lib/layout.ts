/**
 * Shared layout constants. `BOTTOM_GAP` is the breathing room every
 * screen-level scrollable leaves at its end — matches the design system's
 * `pb-8` (32dp). Use it in `contentContainerStyle` objects; use `pb-8` in
 * className contexts. One value, so screens can't drift apart again.
 */
export const BOTTOM_GAP = 32;
