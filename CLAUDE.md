@AGENTS.md

# Roava — Project Context (read this first in every session)

Roava is a production-quality, offline-capable travel companion app (Expo SDK 57, TypeScript strict, Expo Router) built as a **phased learning + portfolio project**. The developer is a 5+ year web dev (React/Next/Node) mastering React Native; act as mentor as well as engineer — explain the _why_, contrast with web, never hide complexity.

## Process contract (non-negotiable)

1. Build **one phase at a time** per `docs/plans/2026-07-04-roava-master-plan.md`. Never start the next phase without the user replying **"Phase Approved"**.
2. Before starting a phase, write its bite-sized task plan to `docs/plans/phase-NN-<name>.md`.
3. End every phase with the 16-point debrief (architecture, trade-offs, RN concepts, perf, a11y, security, interview questions, homework, branch, commit message, improvements).
4. **Every problem + solution gets logged in `docs/JOURNEY.md`** (Problem → Diagnosis → Solution → Lesson) as it happens — no exceptions.
5. Update the **Current Status** section below at every phase boundary.

## Key documents

- `docs/plans/2026-07-04-roava-master-plan.md` — architecture, design system tokens, 18 phases, API map, risks
- `docs/JOURNEY.md` — every problem/solution so far + Windows survival-kit table
- `README.md` — machine setup, run instructions

## Hard rules (from the master plan)

- No API calls from screens — layers: screens → features → hooks → RTK Query → repositories → services
- Every async surface: loading (skeleton) / empty / error / offline states
- Semantic color tokens only (no raw hex in components); Lucide icons only (no emoji icons)
- Touch targets ≥ 48dp; contrast ≥ 4.5:1 in both themes; `accessibilityLabel` on icon-only controls
- Secrets: expo-secure-store; `EXPO_PUBLIC_*` keys are extractable — free quota-capped keys only
- Conventional commits (husky-enforced); one branch per phase
- **Never add `Co-Authored-By` (or any AI-attribution trailer) to commit messages** — user rule, overrides tool defaults; history was scrubbed of them on 2026-07-05
- No custom backend — public APIs only (frontend-first rule)

## Machine facts (Windows 11)

- Node 24.18.0 via nvm — global symlink active (`node --version` → 24.18.0 in any terminal); the per-session PATH workaround is no longer needed (JOURNEY 9.3)
- `JAVA_HOME` = Android Studio JBR (JDK 21); `ANDROID_HOME` set; AVD **`Roava_Pixel`** (Pixel 7, API 36) exists
- Launch emulator/Metro **detached** (`Start-Process`); binary output via `cmd /c` redirects (PowerShell 5.1 corrupts it); see JOURNEY.md survival kit for the rest
- Physical phone connects via **LAN** (`npx expo start`, no --tunnel): Wi-Fi profile is Private + user env var `REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.3` (DHCP IP — goes stale if the router reassigns; JOURNEY 9.3). Fallback if it times out: `--tunnel`, or the elevated port-8081 firewall rule in JOURNEY 9.3. (Historic: Expo Go SDK 57 was sideloaded from expo.dev/go; retired since the Phase 4 dev build.)

## Current Status (update at every phase boundary)

- **Phase 0 (Foundation): APPROVED & merged to main.**
- **Phase 1 (Design System): APPROVED & merged to main.** 10 primitives in `src/components/ui`, tokens as CSS vars, theme store persisted behind `AppStorage`, dev gallery at `/dev-gallery`. Reanimated uses `.get()/.set()` (React Compiler requirement).
- **Phase 2 (Navigation Shell): APPROVED & merged.** Tab shell + AnimatedTabBar, onboarding gate, destination/[id] deep-link target (`roava://` live at Phase 4 dev build).
- **Phase 3 (Data Layer): APPROVED & merged.** Layering locked (screens → hooks → RTK Query queryFn → repositories → services); AppError taxonomy; persistence rehydrated pre-render; useOnline + OfflineBanner; /dev-data harness. Guard RTK errors with `isAppError`. Live-API cache-serving branch proves out in Phase 5.
- **Phase 4 (Auth & Dev Build): APPROVED & merged** (approval recorded 2026-07-05). Branch `feat/phase-04-auth`. **Dev build is now the runtime** (`com.kasir.roava` via `npx expo run:android`, no EAS account; launch with the exp+roava:// dev-client URL or from dev-launcher "recently opened"). Auth: `AuthRepository` (Google impl gated on `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` in .env — user must create Google Cloud OAuth clients, see phase-04 plan; Mock impl active meanwhile), sessions in SecureStore, restore-on-boot thunk, sign-in screen (Google + guest), session-aware Profile with confirm sign-out. Verified: mock sign-in → kill → restore; sign-out wipes; `roava://` deep links live (warm; cold goes via dev-launcher, JOURNEY 7.3). **MMKV deferred** (Nitro not registering under SDK 57, JOURNEY 7.2 — AsyncStorage active behind AppStorage; try mmkv@3 at next native rebuild).
- **Phase 5 (Home & Discovery Feed): APPROVED & merged** (approval recorded 2026-07-05). Branch `feat/phase-05-home`. Live APIs active (user's keys in .env): `LiveDestinationsRepository` (GeoDB top cities host `wft-geo-db.p.rapidapi.com` + Unsplash photos with AppStorage image cache + rendered attribution), env-gated live/mock selection. Home = greeting (session name), trending rail + explore FlashList, skeletons, pull-to-refresh, staggered entrances, stale-cache fallback. **Offline-first proven vs real API** (airplane cold start → cached feed + saved-data chip). Debug tip: `adb logcat -s ReactNativeJS:*` beats Metro log (web-SSR noise, JOURNEY 8.3).
- **Phase 6 (Search): APPROVED & merged (2026-07-05).** Branch `feat/phase-06-search`. Debounced (600ms) GeoDB city search with AbortSignal cancellation through queryFn→axios; prefix-highlight rows; population FiltersSheet (@gorhom bottom-sheet; GestureHandlerRootView + BottomSheetModalProvider now in root layout); search history in AppStorage (max 10, clear button, written on result tap). Verified live: "par" → Paris et al with highlights; filters; history persisted. **After installing packages, restart Metro with --clear** (JOURNEY 9.1).
- **Phase 7 (Destination Details): APPROVED & merged (2026-07-05).** Branch `feat/phase-07-destination`. Detail screen: parallax hero (Reanimated `.get()/.set()`, Unsplash `regular` res via extended CityPhoto cache, rendered attribution, scrim + `on-image`/`scrim` tokens added, Icon gains `filled`), snapshot row w/ independent degradation (OpenWeather live — user's key; local time client-side from GeoDB tz, **normalized `Europe__Paris`→`Europe/Paris`**, JOURNEY 10.1; er-api rate w/ 12h AppStorage TTL + stale-if-error, `HOME_CURRENCY='INR'` const until Phase 15), Overpass POIs (tourism-only — park scan times out on dense cities; **`remark`-as-error guard, JOURNEY 10.3**; one fetch, chips filter locally), favorites slice persisted (WHITELIST + generic `loadSlice`, JOURNEY 10.4), Share w/ `roava://` link, cold deep links back out to Home (`unstable_settings.initialRouteName`). **Verified live on emulator:** Paris + Mumbai, cold-link→back→Home, favorite survives force-stop, airplane audit (uncached → single ErrorState; cached → fully offline screen), share sheet. Deferred: dark-mode visual pass (explicit-light pref active; Phase 15 sweep), parks (Phase 9), 60fps instrumented measure. Phone: arm64 dev-build APK built & served for sideload 2026-07-05 (JOURNEY 9.5 — install confirmation pending).
- **Phase 8 (Weather): APPROVED & merged (2026-07-05).** Branch `feat/phase-08-weather`. Weather screen at `destination/[id]/weather` (route restructured `[id].tsx`→`[id]/index.tsx`; **git needs `--literal-pathspecs` for bracket paths**, JOURNEY 11.4): current header, animated SVG sun arc (lazy-useState clock snapshot — **React Compiler forbids Date.now() in render**, JOURNEY 11.2), 3-hourly rail w/ rain %, client-aggregated daily list w/ comparative temp bands + "so far" partial flag (free tier has no daily endpoint, JOURNEY 11.1), AQI tile (/air_pollution), UV tile (keyless Open-Meteo), 30-min TTL + stale-if-error AppStorage cache. `getFullWeather` composes via allSettled — per-source tile degradation. SnapshotCard promoted to components/shared (+ onPress/chevron); Phase 7 WeatherCard now opens the forecast. **Verified live** (Mumbai monsoon: arc parked pre-sunset, 100% rain rail, AQI Good, UV 1.1) **+ offline disk-cache serve in airplane mode via param deep link** (JOURNEY 11.5). Deferred: stale-banner visual (needs 30-min expiry; logic mirrors currency.ts), keyless mock run (gate pattern shared w/ Phase 7), dark-mode pass (Phase 15).
- **Phase 9 (Maps & Nearby): APPROVED & merged (2026-07-05).** Branch `feat/phase-09-maps`. Map screen at `destination/[id]/map` (param deep-linkable, JOURNEY 11.5): **MapLibre v11 + OpenFreeMap tiles (keyless — Google Maps rejected: billing account required)**, clustered GeoJSONSource (engine-native; cluster tap → `getClusterExpansionZoom`→`easeTo`), POI callout cards, themed style URLs (liberty/dark), on-map OSM attribution, POI-list fallback on map-init failure. Location: expo-location flow, denial = first-class banner + Settings affordance (verified deny AND grant paths). **MMKV registered via mmkv@3 swap — JOURNEY 7.2 CLOSED** (zero storage.ts changes thanks to the interface; one-time AsyncStorage→MMKV boot migration). Rebuild took 12m; nitro-modules removed. **v11 API redesign trap: read installed .d.ts, not tutorials (JOURNEY 12.3); new route types need Metro restart (12.4).** Verified live: Mumbai denial→banner→callout→cluster expansion; Paris ~200 POIs clustered smooth; attribution rendered. Deferred: dark map visual (explicit-light pref; Phase 15), saved favorite pins (coords rarely cached — Phase 13 seam).
- **Phase 10 (Currency): APPROVED & merged (2026-07-05).** Branch `feat/phase-10-currency`. `/currency` converter (Home tool card entry): **whole-table caching per base** (one er-api call = ~160 quotes; cache at the API's response granularity, JOURNEY 13.2), 12h TTL + stale-if-error, `Intl.NumberFormat` money rendering, searchable picker sheet, swap, star-to-save pairs (max 8) + lastPair in persisted `currencySlice`. **Offline showcase proven in decomposed form (JOURNEY 13.1):** zero er-api calls on a fresh boot serving 17-min-old disk rates; airplane-mode saved-pair conversion under the OfflineBanner; pair persistence across force-stop. Honest limits: dev client can't cold-BOOT offline (bundle via Metro — production embeds it); stale badge needs 12h TTL expiry (logic shared with verified weather path). Incidental: one-boot MLRN module registration glitch, resolved by fresh process (JOURNEY 13.3).
- **Phase 11 (Flights): APPROVED & merged (2026-07-05).** Branch `feat/phase-11-flights`. `/flights` search + `/flights/[icao24]` tracker (Home tools row): anonymous OpenSky — **credits bill by query AREA (recon-measured: global = 4 even icao24-filtered), so 30s polls ride a ±1.5° bbox for 1 credit (JOURNEY 14.1)**; searches share one 30s in-memory snapshot (deliberate contrast to currency's disk TTL, 14.3); MapLibre mini-map dot, rotated heading arrow, phase chips derived from ADS-B state, signal-lost fallback holding last fix. **Hard-won rule: never put a value that changes as a RESULT of a query into its RTK arg — cache-key churn = render loop; moving bbox state lives in the repository (JOURNEY 14.2).** Verified: 25 live UAL flights; UAL2137 visibly crossed Wyoming across two polls; zero requests 65s off-screen (focus gating); bogus callsign → coverage-honest empty state.
- **Phase 12 (Trip Planner): APPROVED & merged (2026-07-06).** Branch `feat/phase-12-trips`. The offline-first crown, delivered: Trips tab (list + RHF+Zod create form w/ cross-field date refine), `trip/[id]` with Itinerary (day chips + drag-reorder via draggable-flatlist, order persisted), Budget (category totals, Phase 10 `formatMoney` extracted to `lib/money.ts`), Packing (progress + toggles), Notes (debounced autosave). **First RTK MUTATIONS** — `Trips` tag invalidation over a LOCAL repository; nine writes collapsed into one `updateTrip` + typed `TripCommand` union (JOURNEY 15.2). `TripsRepository` over MMKV: **versioned doc + stepwise migrate seam + corruption-recovery stash** (never discard unparseable bytes, 15.3). **Verified: entire lifecycle in airplane mode incl. process kill → all data intact in drag-reordered order (15.1)**; dev-harness footnote: bundle reload needs network (13.1). Tooling: adb reverse chokes on multi-MB streams — emulator uses LAN Metro; health-check the BUNDLE endpoint, not /status (15.4). Migration seam passthrough-only until schema v2, by design.
- **Phase 13 (Favorites & Offline Hardening): APPROVED & merged (2026-07-06).** Branch `feat/phase-13-favorites`. Favorites tab live (photos offline via expo-image disk cache, tap-through, empty state); swipe-to-remove (RNGH ReanimatedSwipeable) + 5s undo — **`favoriteRestored` reducer restores the EXACT item (original savedAt/position); undo is state restoration, not action replay (JOURNEY 16.1)**; both undo paths verified. **Offline snapshots at the repository seam**: `getDestinationById` writes last-known-good on success, serves `isStale: true` on failure → "saved data" badge; verified cold-store airplane detail w/ independent card degradation (16.2). **Nine-surface airplane audit: all honest** — closed the Phase 8 stale-badge loop live ("saved forecast · updated 5h ago") and discovered MapLibre's ambient tile cache serves visited cities offline; one fix: map floating header hid under the OfflineBanner overlay (absolute z-50) → banner-aware offset (16.3). New trap: airplane toggles silently kill HMR — a fix with NO effect means stale bundle, survival kit rule 15 (16.4).
- **Phase 14 (Profile & Settings): APPROVED & merged (2026-07-06).** Branch `feat/phase-14-profile`. **⚠ Runtime verification DEFERRED** (user stopped emulator/Metro for RAM, 2026-07-06 — per-phase gates are tsc+lint only; ONE consolidated device pass after all phases; checklist lives in each phase plan). Delivered: persisted `settingsSlice.homeCurrency` (WHITELIST 4th member; `HOME_CURRENCY` const → `DEFAULT_HOME_CURRENCY`, all consumers on `selectHomeCurrency`); budget totals now GROUP BY CURRENCY (old entries keep their written currency — primary bucket big, others as captions); Profile Preferences card (theme control + home-currency row reusing CurrencyPickerSheet, label widened); `OfflineDataCard` — `getAllKeys()` added to AppStorage (both engines), five re-downloadable cache buckets counted + surgical clear (never trips/favorites/settings/session/history); About card (8 API attributions, version). React Compiler flagged setState-via-inlined-useCallback in effect — third occurrence of the family; explicit `.then` + live-flag pattern.
- **Phase 15 (Polish & Dark Theme): APPROVED & merged (2026-07-06).** Branch `feat/phase-15-polish`. Dark sweep: **zero non-token colors in src** (14 phases of discipline held); one real find — Input's placeholderTextColor inline RGB literals → palette (native-prop boundaries are where token systems leak, JOURNEY 18.1). `lib/motion.ts` presets with **ReduceMotion.System baked in** (enterDown/enterFade/exitFade/listLayout/enterDownStagger) applied to packing/budget rows, undo bar, trip-section crossfade (key-remount), Home staggers (18.2). Shared `StaleBadge` + `staleAgeLabel` at all 4 badge-shaped stale sites; all 10 EmptyStates pass (18.3). Runtime/visual confirmation on the deferred checklist (plan file).
- **Phase 16 (Production Hardening): APPROVED & merged (2026-07-06).** Branch `feat/phase-16-hardening`. Crash safety: shared `CrashScreen` + expo-router `ErrorBoundary` exports on all 5 param routes + root layout (JOURNEY 19.1). `useSheetBackHandler` inside all 3 sheet components — hardware back dismisses, listener only while open (19.2). Deep-link garbage audit: zero fixes needed, guards were universal; caught "Home currency currency" copy bug (19.3). Release: **keystore + passwords in git-ignored `keys/`** (check-ignore verified), Gradle signing w/ debug fallback (android/ is generated — recipe survives in `docs/release.md`), babel strips console except error/warn in prod, Proguard deliberately off (19.4). Runtime items (crash-screen throw test, back-button walk, garbage links, `assembleRelease`) on the deferred checklist.
- **Phase 17 (README & Portfolio Polish): COMPLETE** — awaiting "Phase Approved". Branch `feat/phase-17-readme`. README rewritten (screenshots grid + "offline receipts" row, feature map, architecture, API table, dev-build run instructions replacing stale Expo Go copy); 11 verified session screenshots in `docs/screenshots/` (each checked by eye — three miscasts caught, one was the 14.2 render-loop error screen); JOURNEY got a 19-chapter index; `docs/demo.md` = five-minute walkthrough script. Then **Phase 18 = the consolidated device verification pass** (all deferred checklists from phases 14–16 + dark walk + assembleRelease + phone arm64 sideload).
- **Google OAuth configured (2026-07-06):** Cloud project + consent screen (testing mode, user as test user) + Android clients for BOTH keystores' SHA-1s + Web client; `EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID` in .env (validated format). AuthRepository auto-selects Google over Mock at next bundle. **Sign-in flow formal test → Phase 18.**
- **Release APK built, installed & running on the phone (2026-07-06)** — Phase 18 items done early: `assembleRelease` (arm64, 21m43s, 62MB), **release-signed (apksigner-verified, matches OAuth-registered SHA-1)**, transferred by file copy, **user-confirmed working on device** — first standalone cold boot (embedded bundle; 13.1 closed on real hardware). War stories: keystore.properties BOM trap (JOURNEY 20.1, survival rule 16); network identity change broke LAN serving — new IP `192.168.0.109`, profile reset to Public, env var updated (20.2, rule 17). Phone-rebuild backlog item RESOLVED (release build supersedes the stale dev sideload).
- Backlog: commitlint graduation. (Done 2026-07-05: LAN fix — JOURNEY 9.3; MMKV swap — JOURNEY 12.1.)
